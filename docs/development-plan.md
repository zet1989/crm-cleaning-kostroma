# 📋 План разработки CRM-системы для клининговой компании

> **Технологический стек:** Next.js 14 + Supabase + OpenRouter AI + shadcn/ui
> **Хостинг:** Beget (VPS/Dedicated для Docker)

---

## 📊 Архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                    Next.js 14 + shadcn/ui                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │  Канбан  │  │ Дашборд  │  │ Настройки│  │ Исполнители  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                        BACKEND                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Supabase                               │   │
│  │  ┌────────────┐  ┌─────────────┐  ┌──────────────────┐   │   │
│  │  │ PostgreSQL │  │    Auth     │  │  Edge Functions  │   │   │
│  │  │   (БД)     │  │  (JWT/RLS)  │  │   (API/Webhooks) │   │   │
│  │  └────────────┘  └─────────────┘  └──────────────────┘   │   │
│  │  ┌────────────┐  ┌─────────────┐                         │   │
│  │  │  Storage   │  │  Realtime   │                         │   │
│  │  │  (Файлы)   │  │ (WebSocket) │                         │   │
│  │  └────────────┘  └─────────────┘                         │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────┬───────────────────────────────────────┐
│                     AI & ИНТЕГРАЦИИ                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    OpenRouter AI                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │   │
│  │  │ Извлечение  │  │ Спам-фильтр │  │  Транскрипция   │   │   │
│  │  │   данных    │  │   (AI)      │  │  (Whisper API)  │   │   │
│  │  │ (GPT-4o)    │  │             │  │                 │   │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │   │
│  │                                                           │   │
│  │  Webhooks: Novofon • Email • Сайт                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ Чек-лист разработки

### Фаза 1: Подготовка и инфраструктура

- [x] **1.1** Настройка локального окружения разработки
  - [x] Инициализация Next.js 14 с App Router
  - [x] Установка и настройка shadcn/ui
  - [x] Настройка TypeScript и ESLint
  - [x] Настройка Tailwind CSS

- [x] **1.2** Настройка Supabase
  - [x] Создание проекта в Supabase (локально для разработки)
  - [x] Настройка переменных окружения
  - [x] Инициализация Supabase CLI

- [x] **1.3** Проектирование базы данных
  - [x] Схема таблиц (users, deals, columns, executors, calls, etc.)
  - [x] Настройка Row Level Security (RLS)
  - [x] Создание миграций

### Фаза 2: Аутентификация и авторизация

- [x] **2.1** Система аутентификации
  - [x] Настройка Supabase Auth
  - [x] Страница входа (Login)
  - [x] Разделение ролей (admin, manager)
  - [x] Поддержка множественных ролей (admin + manager)
  - [x] Middleware для защиты роутов

- [x] **2.2** Управление пользователями (Админ-панель)
  - [x] CRUD для менеджеров
  - [x] CRUD для администраторов
  - [x] CRUD для исполнителей (клинеров)
  - [x] Настройка прав доступа по ролям
  - [x] Настройка индивидуального % зарплаты для менеджеров
  - [x] Страница профиля пользователя

### Фаза 3: Канбан-доска

- [x] **3.1** Базовый функционал канбана
  - [x] Компонент канбан-доски
  - [x] Drag-and-drop между колонками
  - [ ] Добавление/удаление колонок (только админ)
  - [x] Создание сделок

- [x] **3.2** Карточки сделок
  - [x] Краткий вид карточки (сумма, тип, дата, день недели, менеджер, исполнители, описание)
  - [x] Детальный вид сделки (модальное окно)
  - [x] Цветовая маркировка по дням недели
  - [ ] История изменений сделки
  - [x] Множественные исполнители с индивидуальными процентами
  - [x] Множественные менеджеры с индивидуальными процентами
  - [x] Экспорт сделок из колонки в CSV

- [x] **3.3** Повторные клиенты и история звонков
  - [x] Определение повторного клиента по номеру телефона
  - [x] Метка "🔄 Повторный клиент" в карточке
  - [x] Список предыдущих заказов клиента
  - [x] История всех звонков с клиентом (дата, менеджер, длительность)
  - [x] Прослушивание записи звонка
  - [x] Расшифровка звонка
  - [x] Комментарии к звонку

- [x] **3.4** Права доступа в канбане
  - [x] Менеджер: только перемещение сделок
  - [x] Админ: полный доступ (создание, удаление, редактирование)

### Фаза 4: Дашборд аналитики

- [x] **4.1** Финансовая аналитика
  - [x] Выручка по месяцам (график)
  - [x] Количество сделок по статусам
  - [x] Средний чек
  - [x] Конверсия воронки
  - [x] ФОТ исполнителей (сумма выплат клинерам)
  - [x] ФОТ менеджеров (сумма выплат менеджерам)
  - [x] Сумма премий за период
  - [x] Чистая прибыль (выручка минус все выплаты)
  - [x] Выбор произвольного периода

- [x] **4.2** Аналитика исполнителей
  - [x] Заработок исполнителей по месяцам
  - [x] Количество выполненных заказов
  - [x] Статистика отдельного исполнителя
  - [x] Статистика всех исполнителей за период
  - [x] Рейтинг исполнителей
  - [x] Начисленные премии (бонусы)

- [x] **4.3** Аналитика менеджеров
  - [x] Количество лидов по источникам
  - [x] Эффективность менеджеров (конверсия)
  - [x] Статистика отдельного менеджера
  - [x] Статистика всех менеджеров за период
  - [x] Заработок менеджеров за месяц
  - [x] Начисленные премии (бонусы)
  - [x] Отключение менеджера от просмотра аналитики (для новых сотрудников)

- [x] **4.4** Экспорт данных (только админ)
  - [x] Скачивание успешных сделок (CSV)
  - [x] Скачивание неуспешных сделок (CSV)
  - [x] Экспорт из колонки канбана с фильтром по периоду
  - [x] Экспорт статистики исполнителей/менеджеров

### Фаза 5: Система расчёта зарплат

- [x] **5.1** Настройка ставок
  - [x] Глобальный % исполнителей (по умолчанию 40%)
  - [x] Глобальный % менеджеров (по умолчанию 10%)
  - [x] Индивидуальный % для каждого менеджера
  - [x] Индивидуальный % для каждого исполнителя
  - [x] Страница настроек ставок (только админ)

- [x] **5.2** Автоматический расчёт
  - [x] Расчёт зарплаты исполнителей при завершении сделки
  - [x] Распределение между несколькими исполнителями
  - [x] Возможность ручного задания % для каждого исполнителя в сделке
  - [x] Расчёт зарплаты менеджеров
  - [x] Распределение между несколькими менеджерами
  - [x] Возможность ручного задания % для каждого менеджера в сделке
  - [x] Отображение суммы в рублях при выборе процента

- [x] **5.3** Премии
  - [x] Форма назначения премии на заказ
  - [x] Выбор получателя (исполнитель/менеджер)
  - [x] Указание суммы и причины (причина необязательна)
  - [x] Отображение в истории заказа
  - [x] Учёт премий в статистике

- [ ] **5.4** Отчётность по зарплатам
  - [ ] Таблица начислений за период
  - [ ] Экспорт в Excel/CSV
  - [ ] Фильтрация по сотрудникам

### Фаза 6: Интеграции с телефонией и внешними источниками

- [x] **6.1** Интеграция с Novofon (webhooks)
  - [x] Webhook endpoint для приёма событий от Novofon
  - [x] Обработка события NOTIFY_END (завершение звонка)
  - [x] Автоматическое создание лида из звонка
  - [x] Сохранение записи звонка через Novofon API
  - [x] Транскрипция через OpenRouter Whisper
  - [x] Извлечение данных через GPT-4o-mini
  - [x] Поддержка событий: NOTIFY_START, NOTIFY_END, NOTIFY_RECORD
  - [x] Документация (`docs/novofon-integration.md`, `TEST-NOVOFON.md`)

- [x] **6.2** Интеграция с почтой
  - [x] Webhook для входящих заявок с email
  - [x] Автосоздание лидов из писем
  - [x] AI обработка текста письма
  - [x] Персональные webhook URL

- [x] **6.3** Интеграция с сайтом
  - [x] Webhook для форм сайта
  - [x] Автосоздание лидов
  - [x] AI обработка заявок перед созданием сделки
  - [x] Персональные webhook URL

**Статус**: ✅ Выполнено (100%)

### Фаза 7: AI-функционал

- [x] **7.1** Подключение OpenRouter API
  - [x] Настройка API клиента
  - [x] Интерфейс настроек AI в CRM
  - [x] Тестирование подключения
  - [x] Редактируемый System Prompt

- [x] **7.2** AI-обработка заявок
  - [x] Автоматическое извлечение данных из текста
  - [x] Заполнение полей сделки (имя, телефон, адрес, дата)
  - [x] Обработка относительных дат (сегодня, завтра)
  - [x] Определение типа уборки
  - [x] Прямая интеграция без n8n

- [x] **7.3** Транскрипция звонков
  - [x] Интеграция Whisper Large V3 через OpenRouter
  - [x] Автоматическая транскрипция при новом звонке
  - [x] Сохранение расшифровки в метаданных сделки
  - [x] Обработка аудио из Novofon

- [x] **7.4** AI-анализ
  - [x] Извлечение структурированных данных из расшифровки
  - [x] Определение повторного клиента
  - [x] Автоматическое заполнение полей сделки

**Статус**: ✅ Выполнено (100%)

### Фаза 8: Уведомления о новых заявках

- [x] **8.1** Supabase Realtime (в интерфейсе CRM)
  - [x] Настройка WebSocket подключения
  - [x] Подписка на INSERT в таблице deals
  - [x] Toast-уведомления о новых лидах
  - [x] Звуковой сигнал при новой заявке
  - [x] Управление звуком в header
  - [x] Синхронизация канбана между пользователями в реальном времени

- [ ] **8.2** Telegram Bot
  - [ ] Создание бота через @BotFather
  - [ ] Таблица telegram_subscriptions в Supabase
  - [ ] Edge Function для отправки уведомлений
  - [ ] Database Trigger при создании лида
  - [ ] Форматирование сообщений (телефон, источник, время)
  - [ ] Кнопка быстрого перехода в CRM
  - [ ] Страница настройки Telegram в профиле пользователя

- [ ] **8.3** Web Push (браузерные уведомления)
  - [ ] Service Worker для фоновой работы
  - [ ] Генерация VAPID ключей
  - [ ] Таблица push_subscriptions в Supabase
  - [ ] Edge Function для отправки push
  - [ ] Database Trigger при создании лида

### Фаза 9: SMS-рассылка

- [ ] **9.1** SMS для клиентов
  - [ ] Интеграция с SMS-провайдером (SMS.ru / SMSC)
  - [ ] Настройка шаблонов сообщений
  - [ ] Автоотправка запроса на отзыв после выполнения
  - [ ] Логирование отправленных SMS

### Фаза 10: Настройки и администрирование

- [ ] **10.1** Страница настроек
  - [ ] Настройки SMS-рассылки
  - [ ] Настройки интеграций (API ключи)
  - [ ] Настройки уведомлений (Telegram, Web Push)
  - [ ] Типы уборки (справочник)
  - [ ] Настройки зарплат (глобальные %)

- [ ] **10.2** Логирование и аудит
  - [ ] Журнал действий пользователей
  - [ ] История изменений сделок
  - [ ] Резервное копирование

### Фаза 11: Тестирование и оптимизация

- [ ] **11.1** Тестирование
  - [ ] Unit-тесты критичных функций
  - [ ] E2E тесты основных сценариев
  - [ ] Нагрузочное тестирование

- [ ] **11.2** Оптимизация
  - [ ] Оптимизация запросов к БД
  - [ ] Кэширование
  - [ ] Lazy loading компонентов

### Фаза 12: Деплой на Beget

- [x] **12.1** Подготовка к деплою
  - [x] Настройка production переменных
  - [x] Docker конфигурация (docker-compose.prod.yml)
  - [x] Dockerfile для production сборки
  - [x] Nginx конфигурация
  - [x] Скрипты деплоя и бэкапа
  - [x] Документация по деплою

- [ ] **12.2** Развертывание
  - [ ] Покупка VPS на Beget (2 ядра / 2 ГБ / 30 ГБ)
  - [ ] Установка Docker и Docker Compose
  - [ ] Настройка файрвола и безопасности
  - [ ] Клонирование проекта на сервер
  - [ ] Настройка SSL (Let's Encrypt)
  - [ ] Запуск через Docker Compose
  - [ ] Настройка Novofon webhooks (через техподдержку)
  - [ ] Настройка автоматических бэкапов
  - [ ] Тестирование на production

**Статус**: ✅ Готово к деплою (Фаза 12.1 выполнена)

**Документация:**
- [Деплой на Beget](./beget-deployment.md) - пошаговая инструкция
- [Требования к серверу](./server-requirements.md) - анализ конфигураций

---

## 📁 Структура проекта

```
crm.loc/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # Группа для авторизации
│   │   ├── login/
│   │   └── layout.tsx
│   ├── (dashboard)/              # Защищённые страницы
│   │   ├── kanban/               # Канбан-доска
│   │   ├── analytics/            # Дашборд аналитики
│   │   ├── executors/            # Исполнители
│   │   ├── settings/             # Настройки
│   │   └── layout.tsx
│   ├── api/                      # API Routes
│   │   ├── webhooks/             # Вебхуки (Novofon, сайт)
│   │   └── ai/                   # AI эндпоинты
│   ├── layout.tsx
│   └── page.tsx
├── components/                   # React компоненты
│   ├── ui/                       # shadcn/ui компоненты
│   ├── kanban/                   # Компоненты канбана
│   │   ├── board.tsx
│   │   ├── column.tsx
│   │   ├── card.tsx
│   │   └── deal-modal.tsx
│   ├── analytics/                # Графики и дашборды
│   └── shared/                   # Общие компоненты
├── lib/                          # Утилиты и конфигурации
│   ├── supabase/
│   │   ├── client.ts
│   │   ├── server.ts
│   │   └── middleware.ts
│   ├── ai/
│   │   └── openrouter.ts
│   └── utils.ts
├── types/                        # TypeScript типы
│   └── database.ts
├── hooks/                        # React хуки
├── supabase/                     # Supabase конфигурация
│   ├── migrations/               # SQL миграции
│   ├── functions/                # Edge Functions
│   └── config.toml
├── docs/                         # Документация
│   ├── requirements.md
│   ├── development-plan.md
│   └── deployment-guide.md
└── n8n/                          # n8n конфигурация
    └── workflows/                # Экспорт workflow
```

---

## 🗄️ Схема базы данных

```sql
-- Пользователи (расширение auth.users)
CREATE TABLE public.profiles (
    id UUID PRIMARY KEY REFERENCES auth.users(id),
    roles TEXT[] NOT NULL DEFAULT ARRAY['manager'], -- Массив ролей: ['admin'], ['manager'], ['admin', 'manager']
    full_name TEXT NOT NULL,
    phone TEXT,
    salary_percent DECIMAL(5,2) DEFAULT 10.00, -- Индивидуальный % зарплаты для менеджера
    can_view_analytics BOOLEAN DEFAULT true,    -- Доступ к дашборду аналитики (можно отключить для новых сотрудников)
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Исполнители (клинеры)
CREATE TABLE public.executors (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    full_name TEXT NOT NULL,
    phone TEXT NOT NULL,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Колонки канбана
CREATE TABLE public.columns (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title TEXT NOT NULL,
    position INTEGER NOT NULL,
    color TEXT,
    is_paid BOOLEAN DEFAULT false, -- Колонка "Оплачено" для триггера расчёта зарплат
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Типы уборки
CREATE TABLE public.cleaning_types (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    base_price DECIMAL(10,2),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Сделки
CREATE TABLE public.deals (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    column_id UUID REFERENCES public.columns(id),
    manager_id UUID REFERENCES public.profiles(id),
    
    -- Контактные данные
    client_name TEXT,
    client_phone TEXT NOT NULL,
    client_email TEXT,
    address TEXT,
    
    -- Детали сделки
    cleaning_type_id UUID REFERENCES public.cleaning_types(id),
    scheduled_date DATE,
    scheduled_time TIME,
    amount DECIMAL(10,2),
    description TEXT,
    short_info TEXT,
    
    -- Расчёт зарплат
    executors_total_earnings DECIMAL(10,2), -- Общая сумма для исполнителей
    manager_earnings DECIMAL(10,2),          -- Зарплата менеджера
    is_salary_calculated BOOLEAN DEFAULT false, -- Флаг расчёта зарплаты
    
    -- Источник лида
    source TEXT CHECK (source IN ('website', 'email', 'call', 'manual')),
    source_data JSONB,
    
    -- AI данные
    call_transcript TEXT,
    ai_analysis JSONB,
    is_spam BOOLEAN DEFAULT false,
    
    -- Мета
    position INTEGER,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Связь сделок с исполнителями
CREATE TABLE public.deal_executors (
    deal_id UUID REFERENCES public.deals(id) ON DELETE CASCADE,
    executor_id UUID REFERENCES public.executors(id) ON DELETE CASCADE,
    earnings DECIMAL(10,2),         -- Рассчитанный заработок
    earnings_percent DECIMAL(5,2), -- % от общей суммы исполнителей (для ручного распределения)
    PRIMARY KEY (deal_id, executor_id)
);

-- Премии
CREATE TABLE public.bonuses (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deal_id UUID REFERENCES public.deals(id) ON DELETE CASCADE,
    recipient_type TEXT NOT NULL CHECK (recipient_type IN ('executor', 'manager')),
    executor_id UUID REFERENCES public.executors(id), -- Если премия исполнителю
    manager_id UUID REFERENCES public.profiles(id),   -- Если премия менеджеру
    amount DECIMAL(10,2) NOT NULL,
    reason TEXT,
    created_by UUID REFERENCES public.profiles(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Звонки
CREATE TABLE public.calls (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deal_id UUID REFERENCES public.deals(id),
    client_phone TEXT NOT NULL,              -- Номер клиента (для поиска истории)
    manager_id UUID REFERENCES public.profiles(id), -- Кто звонил/принял звонок
    novofon_call_id TEXT,
    direction TEXT CHECK (direction IN ('inbound', 'outbound')), -- Входящий/исходящий
    duration INTEGER,                         -- Длительность в секундах
    recording_url TEXT,                       -- Ссылка на запись
    transcript TEXT,                          -- Расшифровка
    ai_summary TEXT,                          -- AI краткое содержание
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Комментарии к звонкам
CREATE TABLE public.call_comments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    call_id UUID REFERENCES public.calls(id) ON DELETE CASCADE,
    user_id UUID REFERENCES public.profiles(id),
    comment TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- История изменений сделок
CREATE TABLE public.deal_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deal_id UUID REFERENCES public.deals(id) ON DELETE CASCADE,
    user_id UUID REFERENCES public.profiles(id),
    action TEXT NOT NULL,
    changes JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Настройки системы
CREATE TABLE public.settings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    key TEXT UNIQUE NOT NULL,
    value JSONB NOT NULL,
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Значения по умолчанию для настроек:
-- { "executors_percent": 40, "managers_percent": 10 }

-- Отзывы
CREATE TABLE public.reviews (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deal_id UUID REFERENCES public.deals(id),
    rating INTEGER CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Telegram подписки для уведомлений
CREATE TABLE public.telegram_subscriptions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES public.profiles(id) ON DELETE CASCADE,
    chat_id BIGINT NOT NULL,
    is_active BOOLEAN DEFAULT true,
    notify_new_leads BOOLEAN DEFAULT true,
    notify_deal_updates BOOLEAN DEFAULT false,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id)
);

-- Web Push подписки
CREATE TABLE public.push_subscriptions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES public.profiles(id) ON DELETE CASCADE,
    endpoint TEXT NOT NULL,
    p256dh TEXT NOT NULL,
    auth TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, endpoint)
);

-- SMS логи
CREATE TABLE public.sms_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deal_id UUID REFERENCES public.deals(id),
    phone TEXT NOT NULL,
    message TEXT NOT NULL,
    status TEXT CHECK (status IN ('pending', 'sent', 'delivered', 'failed')),
    provider_response JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🎨 Дизайн карточки сделки в канбане

### Краткий вид (в колонке)

```
┌─────────────────────────────────────┐
│ 💰 15 000 ₽        📅 Пн, 15.12.25  │  ← Сумма и дата с днём недели
├─────────────────────────────────────┤
│ 🧹 Уборка после ремонта             │  ← Тип уборки
├─────────────────────────────────────┤
│ 👤 Иванов И.                        │  ← Менеджер
│ 👷 Петрова А., Сидоров В.           │  ← Исполнители
├─────────────────────────────────────┤
│ +7 910 101-09-00                    │
│ ул. Радионова 178 кв 14             │  ← Краткое описание
│ окна и внутрянку посчитать на месте │
└─────────────────────────────────────┘
```

### Цветовая маркировка по дням

- 🔴 Понедельник - красная плашка
- 🟠 Вторник - оранжевая
- 🟡 Среда - жёлтая
- 🟢 Четверг - зелёная
- 🔵 Пятница - синяя
- 🟣 Суббота - фиолетовая
- ⚪ Воскресенье - серая

---

## ⏱️ Примерные сроки разработки

| Фаза | Описание | Срок |
|------|----------|------|
| 1 | Подготовка и инфраструктура | 2-3 дня |
| 2 | Аутентификация и авторизация | 2-3 дня |
| 3 | Канбан-доска | 4-5 дней |
| 4 | Дашборд аналитики | 3-4 дня |
| 5 | Интеграции n8n | 3-4 дня |
| 6 | AI-функционал | 3-4 дня |
| 7 | SMS и уведомления | 2-3 дня |
| 8 | Настройки | 2-3 дня |
| 9 | Тестирование | 2-3 дня |
| 10 | Деплой | 1-2 дня |
| **Итого** | | **~25-35 дней** |

---

## 🚀 Следующий шаг

Начинаем с **Фазы 1** - настройка проекта и инфраструктуры.
