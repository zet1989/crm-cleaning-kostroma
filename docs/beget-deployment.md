# 🚀 Деплой CRM на Beget VPS

## 📋 Требования к серверу

### Рекомендуемая конфигурация

**Для продакшена:**
- ✅ **2 ядра CPU** (минимум 1 ядро)
- ✅ **2 ГБ RAM** (минимум 1 ГБ, но будет тесно)
- ✅ **30 ГБ NVMe** (минимум 10 ГБ)
- ✅ **250 МБ/сек канал**

**Стоимость:** 660₽/месяц

**Почему эта конфигурация:**
- 🐘 **PostgreSQL (Supabase)** - 512 МБ RAM (база данных)
- 🟢 **Next.js приложение** - 512 МБ RAM (веб-сервер)
- 🔄 **Nginx** - 128 МБ RAM (reverse proxy)
- 📊 **Мониторинг + резерв** - ~512 МБ RAM
- 💾 **Хранение**: база данных, записи звонков, логи, бэкапы

---

## 🛠️ Подготовка к деплою

### Шаг 1: Покупка VPS на Beget

1. Перейдите на [beget.com/ru/vps](https://beget.com/ru/vps)
2. Выберите конфигурацию **2 ядра / 2 ГБ / 30 ГБ** (660₽/мес)
3. Регион: **Россия, Санкт-Петербург**
4. ОС: **Ubuntu 22.04 LTS**
5. Оплатите и дождитесь создания сервера
6. Получите данные доступа (IP, root пароль)

---

### Шаг 2: Подключение к серверу

```bash
# Подключитесь по SSH
ssh root@ваш-ip-адрес

# Обновите систему
apt update && apt upgrade -y

# Установите необходимые пакеты
apt install -y curl git htop ufw fail2ban
```

---

### Шаг 3: Установка Docker и Docker Compose

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install -y docker-compose

# Проверка установки
docker --version
docker-compose --version

# Добавление автозапуска
systemctl enable docker
systemctl start docker
```

---

### Шаг 4: Настройка файрвола

```bash
# Разрешаем SSH, HTTP, HTTPS
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp

# Включаем файрвол
ufw enable

# Проверяем статус
ufw status
```

---

### Шаг 5: Настройка домена

1. В панели управления доменом добавьте A-запись:
   ```
   @ (или ваш поддомен) → IP-адрес вашего VPS
   ```

2. Дождитесь обновления DNS (до 24 часов, обычно 1-2 часа)

3. Проверьте:
   ```bash
   ping ваш-домен.com
   ```

---

### Шаг 6: Клонирование проекта

```bash
# Создайте директорию для проекта
mkdir -p /opt/crm
cd /opt/crm

# Клонируйте репозиторий (или загрузите архив)
git clone https://github.com/ваш-репозиторий/crm.git .

# Или загрузите через SCP:
# scp -r C:\OSPanel\domains\crm.loc root@ваш-ip:/opt/crm/
```

---

### Шаг 7: Настройка переменных окружения

```bash
# Создайте .env файл
nano .env.production
```

Добавьте:

```env
# Production окружение
NODE_ENV=production

# Next.js
NEXT_PUBLIC_SITE_URL=https://ваш-домен.com

# Supabase (используйте облачный Supabase или self-hosted)
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key

# Novofon API
NOVOFON_APP_ID=appid_1834174
NOVOFON_SECRET=ваш-секретный-ключ

# OpenRouter AI
OPENROUTER_API_KEY=ваш-ключ-openrouter

# База данных (если используете локальный PostgreSQL)
DATABASE_URL=postgresql://postgres:пароль@localhost:5432/crm

# SSL сертификаты (Let's Encrypt)
LETSENCRYPT_EMAIL=ваш-email@example.com
```

---

### Шаг 8: Запуск через Docker Compose

```bash
# Запустите все сервисы
docker-compose -f docker-compose.prod.yml up -d

# Проверьте статус
docker-compose ps

# Просмотр логов
docker-compose logs -f next-app
```

---

### Шаг 9: Настройка SSL (Let's Encrypt)

```bash
# Установка Certbot
apt install -y certbot python3-certbot-nginx

# Получение сертификата
certbot --nginx -d ваш-домен.com

# Автообновление сертификата (добавляется автоматически в cron)
certbot renew --dry-run
```

---

### Шаг 10: Настройка Nginx

Nginx уже настроен в Docker Compose, но проверьте конфигурацию:

```nginx
# /opt/crm/nginx/nginx.conf

server {
    listen 80;
    server_name ваш-домен.com;

    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name ваш-домен.com;

    ssl_certificate /etc/letsencrypt/live/ваш-домен.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ваш-домен.com/privkey.pem;

    location / {
        proxy_pass http://next-app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 🔧 Обслуживание

### Просмотр логов

```bash
# Логи Next.js
docker-compose logs -f next-app

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи Nginx
docker-compose logs -f nginx
```

### Перезапуск сервисов

```bash
# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart next-app
```

### Обновление приложения

```bash
cd /opt/crm

# Остановите сервисы
docker-compose down

# Обновите код
git pull origin main

# Пересоберите образы
docker-compose build

# Запустите снова
docker-compose up -d
```

### Резервное копирование

```bash
# Создайте скрипт бэкапа
nano /opt/backup-crm.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# Создайте директорию для бэкапов
mkdir -p $BACKUP_DIR

# Бэкап базы данных
docker exec crm-postgres pg_dump -U postgres crm > $BACKUP_DIR/db_$DATE.sql

# Бэкап файлов
tar -czf $BACKUP_DIR/files_$DATE.tar.gz /opt/crm

# Удалите старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "*.sql" -mtime +30 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $DATE"
```

```bash
# Сделайте скрипт исполняемым
chmod +x /opt/backup-crm.sh

# Добавьте в cron (каждый день в 3:00)
crontab -e
```

Добавьте строку:
```
0 3 * * * /opt/backup-crm.sh >> /var/log/crm-backup.log 2>&1
```

---

## 🎯 Мониторинг

### Установка мониторинга ресурсов

```bash
# Установите htop для мониторинга
apt install -y htop

# Запустите
htop
```

### Мониторинг места на диске

```bash
# Проверка использования диска
df -h

# Размер директорий
du -sh /opt/crm/*
```

### Проверка производительности

```bash
# Использование CPU и памяти контейнерами
docker stats
```

---

## ⚠️ Troubleshooting

### Проблема: сервис не запускается

```bash
# Проверьте логи
docker-compose logs

# Проверьте статус контейнеров
docker-compose ps
```

### Проблема: сайт недоступен

```bash
# Проверьте Nginx
systemctl status nginx

# Проверьте файрвол
ufw status

# Проверьте открытые порты
netstat -tlnp | grep :80
netstat -tlnp | grep :443
```

### Проблема: база данных не подключается

```bash
# Проверьте PostgreSQL
docker exec -it crm-postgres psql -U postgres

# Проверьте подключение
\l  # список баз
\c crm  # подключиться к базе
\dt  # список таблиц
```

---

## 📊 Мониторинг производительности

### Ожидаемая нагрузка

**Для CRM с 5-10 пользователями:**
- CPU: 20-40% в среднем
- RAM: 1.2-1.5 ГБ (из 2 ГБ)
- Диск: 10-15 ГБ (база + логи + записи звонков)

**При пиковой нагрузке:**
- CPU: до 80%
- RAM: до 1.8 ГБ

**Рекомендация масштабирования:**
- При >50 пользователей → 4 ядра / 6 ГБ RAM
- При >100 пользователей → 6 ядер / 12 ГБ RAM

---

## ✅ Чеклист деплоя

- [ ] VPS куплен и настроен
- [ ] Docker установлен
- [ ] Домен настроен и привязан к IP
- [ ] Проект загружен на сервер
- [ ] `.env.production` настроен
- [ ] Docker Compose запущен
- [ ] SSL сертификат установлен
- [ ] Novofon webhooks настроены
- [ ] Backup скрипт настроен
- [ ] Мониторинг работает
- [ ] Тестовый звонок/заявка проверены

---

## 🎉 Готово!

Ваша CRM развёрнута и готова к работе!

**URL:** https://ваш-домен.com
