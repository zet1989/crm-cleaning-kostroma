# 🚀 Руководство по деплою и обновлению CRM

> Подробная инструкция для начинающих: как работать с проектом после переноса на хостинг

---

## 📋 Содержание

1. [Подготовка к деплою](#подготовка-к-деплою)
2. [Первоначальный деплой](#первоначальный-деплой)
3. [Рабочий процесс разработки](#рабочий-процесс-разработки)
4. [Обновление проекта на сервере](#обновление-проекта-на-сервере)
5. [Работа с миграциями БД](#работа-с-миграциями-бд)
6. [Частые сценарии](#частые-сценарии)
7. [Решение проблем](#решение-проблем)

---

## 🎯 Подготовка к деплою

### Шаг 1: Установка Git

**Windows:**
1. Скачай [Git for Windows](https://git-scm.com/download/win)
2. Установи с настройками по умолчанию
3. Открой PowerShell и проверь:
```bash
git --version
```

**Первоначальная настройка:**
```bash
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"
```

### Шаг 2: Создание репозитория

**Вариант A: GitHub (публичный/приватный)**

1. Зайди на [github.com](https://github.com)
2. Нажми "New repository"
3. Название: `cleaning-crm`
4. Выбери **Private** (приватный)
5. НЕ создавай README, .gitignore (у нас уже есть)

**Вариант B: GitLab, Bitbucket** - аналогично

### Шаг 3: Инициализация Git в проекте

Открой PowerShell в папке проекта:

```bash
# Переходим в папку проекта
cd C:\OSPanel\domains\crm.loc

# Инициализируем Git
git init

# Создаём .gitignore (если нет)
# Он уже должен быть в проекте Next.js
```

Проверь, что `.gitignore` содержит:
```
# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/
build
dist/

# production
*.log

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Supabase
.branches
.temp
```

### Шаг 4: Первый коммит

```bash
# Добавляем все файлы в отслеживание
git add .

# Создаём первый коммит
git commit -m "Initial commit: CRM система готова к деплою"

# Связываем с удалённым репозиторием (замени URL на свой)
git remote add origin https://github.com/твой-username/cleaning-crm.git

# Отправляем код на GitHub
git push -u origin main
```

Если ошибка с `main`/`master`:
```bash
# Переименуй ветку в main
git branch -M main
git push -u origin main
```

---

## 🌐 Первоначальный деплой

### Шаг 1: Подготовка сервера на Beget

**1.1 Подключение по SSH**

1. В панели Beget включи SSH-доступ
2. Запомни данные: логин, пароль, хост
3. Подключись через PowerShell:

```bash
ssh твой-логин@твой-хост.beget.tech
```

**1.2 Установка Node.js** (если не установлен)

```bash
# Проверь версию Node.js
node -v

# Если нет или старая версия, установи через nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
```

**1.3 Установка PM2** (менеджер процессов)

```bash
npm install -g pm2
```

### Шаг 2: Клонирование проекта на сервер

```bash
# Переходим в директорию сайтов (обычно это ~/public_html или ~/www)
cd ~/public_html

# Клонируем проект
git clone https://github.com/твой-username/cleaning-crm.git crm

# Переходим в папку проекта
cd crm

# Устанавливаем зависимости
npm install
```

### Шаг 3: Настройка переменных окружения

Создай файл `.env.production` на сервере:

```bash
nano .env.production
```

Заполни переменные (production-значения):

```env
# Supabase (production)
NEXT_PUBLIC_SUPABASE_URL=https://твой-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=твой-anon-key
SUPABASE_SERVICE_ROLE_KEY=твой-service-role-key

# OpenRouter AI
OPENROUTER_API_KEY=твой-openrouter-key

# Novofon
NOVOFON_ACCESS_TOKEN=твой-токен

# URL
NEXT_PUBLIC_APP_URL=https://crm.твойсайт.ru
```

Сохрани: `Ctrl+X`, затем `Y`, затем `Enter`

### Шаг 4: Сборка и запуск

```bash
# Собираем production-версию
npm run build

# Запускаем через PM2
pm2 start npm --name "crm" -- start

# Сохраняем конфигурацию PM2 (чтобы автозапуск при перезагрузке)
pm2 save
pm2 startup
```

### Шаг 5: Настройка Nginx (проксирование)

Создай конфигурацию Nginx:

```bash
nano /etc/nginx/sites-available/crm
```

```nginx
server {
    listen 80;
    server_name crm.твойсайт.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируй конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/crm /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Шаг 6: SSL-сертификат (Let's Encrypt)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d crm.твойсайт.ru
```

### Шаг 7: Настройка Novofon Webhooks

```bash
# Webhooks настраиваются через техподдержку Novofon
# 1. Получите webhook URL из CRM (Настройки → Интеграции)
# 2. Напишите в support@novofon.com с запросом:
#    - Внутренний номер АТС: [ваш номер]
#    - Webhook URL: https://your-domain.com/api/webhooks/novofon
#    - События: NOTIFY_END

# Сохраняем
pm2 save
```

**✅ Готово! CRM работает на https://crm.твойсайт.ru**

---

## 💻 Рабочий процесс разработки

### Ежедневная работа (локально)

```bash
# 1. Включаем локальный сервер
npm run dev

# 2. Работаем над кодом (добавляем функции, правим баги)

# 3. Тестируем локально в браузере
```

### Когда готов к обновлению production

**Шаг 1: Коммит изменений**

```bash
# Смотрим что изменилось
git status

# Добавляем файлы (выборочно или все)
git add src/app/kanban/page.tsx    # конкретный файл
# или
git add .                           # все изменения

# Создаём коммит с описанием
git commit -m "Добавил фильтр по датам в канбане"
```

**Шаг 2: Отправка на GitHub**

```bash
git push origin main
```

**Шаг 3: Обновление на сервере**

```bash
# Подключаемся к серверу
ssh твой-логин@твой-хост.beget.tech

# Переходим в папку проекта
cd ~/public_html/crm

# Скачиваем изменения
git pull origin main

# Устанавливаем новые зависимости (если добавлялись)
npm install

# Пересобираем проект
npm run build

# Перезапускаем приложение
pm2 restart crm
```

**✅ Изменения применены!**

---

## 🗄️ Работа с миграциями БД

### Важно понять:

- **Код (Git)** - это файлы проекта (TypeScript, React компоненты)
- **БД (Supabase миграции)** - это структура таблиц, колонок, RLS-политик

Они обновляются **отдельно**, но **синхронно**.

### Сценарий: Добавление новой колонки в таблицу

#### 1. Локальная разработка

**Шаг 1: Создаём миграцию**

```bash
# Локально создаём файл миграции
supabase migration new add_bonus_column_to_deals
```

Откроется файл `supabase/migrations/YYYYMMDD_add_bonus_column_to_deals.sql`

Заполни его:

```sql
-- Добавляем колонку bonus в таблицу deals
ALTER TABLE public.deals 
ADD COLUMN bonus DECIMAL(10,2) DEFAULT 0;

-- Добавляем комментарий
COMMENT ON COLUMN public.deals.bonus IS 'Дополнительная премия к сделке';
```

**Шаг 2: Применяем миграцию локально**

```bash
# Применяем к локальной БД
supabase db reset

# Проверяем что всё работает
npm run dev
```

**Шаг 3: Обновляем код под новую колонку**

Например, добавляем поле в форму создания сделки:

```typescript
// src/app/kanban/page.tsx
<Input 
  name="bonus" 
  type="number" 
  placeholder="Премия" 
/>
```

**Шаг 4: Тестируем всё вместе**

Проверяешь что всё работает локально.

#### 2. Деплой миграции и кода

**Вариант A: Через Supabase Dashboard (для новичков)**

1. Зайди на [supabase.com](https://supabase.com) → твой проект
2. Перейди в **SQL Editor**
3. Скопируй содержимое миграции
4. Вставь и нажми **RUN**
5. Проверь в **Table Editor** что колонка появилась

**Вариант B: Через Supabase CLI (для продвинутых)**

```bash
# Локально
supabase db push --db-url "postgresql://postgres:[твой-пароль]@db.[твой-проект].supabase.co:5432/postgres"
```

**Шаг 5: Деплой кода**

```bash
# Коммитим миграцию И код
git add supabase/migrations/YYYYMMDD_add_bonus_column_to_deals.sql
git add src/app/kanban/page.tsx
git commit -m "Добавил поле премии в сделки"
git push origin main

# На сервере
ssh твой-логин@твой-хост.beget.tech
cd ~/public_html/crm
git pull origin main
npm run build
pm2 restart crm
```

### Важные правила миграций

✅ **МОЖНО:**
- Добавлять новые таблицы
- Добавлять новые колонки с DEFAULT значением
- Создавать индексы
- Добавлять RLS-политики

❌ **НЕЛЬЗЯ (опасно для production):**
- Удалять колонки с данными (сначала бэкап!)
- Менять тип колонки (может сломать данные)
- Удалять таблицы

**Если нужно удалить колонку:**

1. Сначала удали все зависимости в коде
2. Задеплой код
3. Потом удали колонку в БД

---

## 🎬 Частые сценарии

### Сценарий 1: Добавил новую страницу

```bash
# 1. Локально создаёшь src/app/reports/page.tsx
# 2. Тестируешь
npm run dev

# 3. Коммитишь
git add src/app/reports/page.tsx
git commit -m "Добавил страницу отчётов"
git push origin main

# 4. На сервере
ssh user@host
cd ~/public_html/crm
git pull origin main
npm run build
pm2 restart crm
```

### Сценарий 2: Исправил баг

```bash
# 1. Локально правишь файл
# 2. Тестируешь
git add src/components/kanban/deal-card.tsx
git commit -m "Исправил баг с отображением даты"
git push origin main

# 3. На сервере
ssh user@host
cd ~/public_html/crm
git pull origin main
npm run build
pm2 restart crm
```

### Сценарий 3: Установил новую библиотеку

```bash
# 1. Локально
npm install date-fns
# Используешь в коде

# 2. Коммитишь package.json и код
git add package.json package-lock.json src/...
git commit -m "Добавил библиотеку date-fns для работы с датами"
git push origin main

# 3. На сервере (важно: npm install!)
ssh user@host
cd ~/public_html/crm
git pull origin main
npm install          # ← обязательно!
npm run build
pm2 restart crm
```

### Сценарий 4: Изменил переменные окружения

```bash
# 1. На сервере редактируем .env.production
ssh user@host
cd ~/public_html/crm
nano .env.production
# Меняем значения
# Сохраняем: Ctrl+X → Y → Enter

# 2. Перезапускаем (без git pull)
pm2 restart crm
```

### Сценарий 5: Откат к предыдущей версии

```bash
# Локально смотрим историю
git log --oneline

# Откатываемся на конкретный коммит
git reset --hard abc1234

# Или откатываем последний коммит
git reset --hard HEAD~1

# На сервере делаем то же самое
ssh user@host
cd ~/public_html/crm
git fetch origin
git reset --hard origin/main
npm run build
pm2 restart crm
```

---

## 🛠️ Полезные команды

### Git команды

```bash
# Посмотреть статус (что изменилось)
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения в файле
git diff src/app/kanban/page.tsx

# Отменить изменения в файле (до коммита)
git checkout src/app/kanban/page.tsx

# Создать новую ветку для эксперимента
git checkout -b feature/new-reports
git add .
git commit -m "Эксперимент с отчётами"
git push origin feature/new-reports

# Вернуться на main
git checkout main
```

### PM2 команды

```bash
# Посмотреть запущенные процессы
pm2 list

# Логи приложения
pm2 logs crm

# Остановить
pm2 stop crm

# Запустить
pm2 start crm

# Перезапустить
pm2 restart crm

# Удалить из PM2
pm2 delete crm

# Очистить логи
pm2 flush
```

### Полезные алиасы (для ускорения)

Добавь в `~/.bashrc` на сервере:

```bash
nano ~/.bashrc
```

```bash
# Алиасы для CRM
alias crm-update='cd ~/public_html/crm && git pull origin main && npm install && npm run build && pm2 restart crm'
alias crm-logs='pm2 logs crm'
alias crm-status='pm2 list'
```

Сохрани и активируй:

```bash
source ~/.bashrc
```

Теперь можно обновлять одной командой:

```bash
crm-update
```

---

## 🚨 Решение проблем

### Проблема 1: Конфликт при git pull

```bash
# Если на сервере случайно отредактировал файлы
git stash          # Сохраняем изменения
git pull origin main
git stash pop      # Восстанавливаем изменения (если нужно)
```

### Проблема 2: Приложение не запускается

```bash
# Смотрим логи
pm2 logs crm --lines 100

# Проверяем переменные окружения
cat .env.production

# Проверяем порт (должен быть свободен)
netstat -tulpn | grep :3000

# Перезапускаем с нуля
pm2 delete crm
npm run build
pm2 start npm --name "crm" -- start
```

### Проблема 3: Миграция не применилась

```bash
# Проверь на Supabase Dashboard → SQL Editor
SELECT * FROM information_schema.columns 
WHERE table_name = 'deals';

# Примени миграцию вручную
# Копируешь SQL из файла миграции → SQL Editor → RUN
```

### Проблема 4: Забыл пароль от сервера

1. Зайди в панель Beget
2. Раздел SSH → Сбросить пароль

### Проблема 5: Node.js процесс съедает память

```bash
# Смотрим потребление
pm2 monit

# Если проблема с утечкой памяти
pm2 restart crm --max-memory-restart 500M
```

---

## 📊 Чек-лист перед деплоем

Перед каждым обновлением на production проверь:

- [ ] Код работает локально без ошибок
- [ ] Запустил `npm run build` локально (проверка TypeScript)
- [ ] Миграции применены локально и протестированы
- [ ] Коммит имеет понятное описание
- [ ] Нет файлов `.env` в коммите
- [ ] В package.json нет `--save-dev` зависимостей в `dependencies`
- [ ] Есть бэкап БД (если меняешь схему)

---

## 🎓 Резюме: Простой рабочий процесс

```
┌─────────────────────────────────────────────┐
│         ЛОКАЛЬНАЯ РАЗРАБОТКА                │
│  1. npm run dev                             │
│  2. Пишешь код                              │
│  3. Тестируешь                              │
│  4. git add . && git commit -m "..."        │
│  5. git push origin main                    │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│      ОБНОВЛЕНИЕ НА СЕРВЕРЕ                  │
│  1. ssh user@host                           │
│  2. cd ~/public_html/crm                    │
│  3. git pull origin main                    │
│  4. npm install (если package.json изменён) │
│  5. npm run build                           │
│  6. pm2 restart crm                         │
└─────────────────────────────────────────────┘
```

### Работа с БД отдельно:

```
┌─────────────────────────────────────────────┐
│         МИГРАЦИЯ БД (локально)              │
│  1. supabase migration new ...              │
│  2. Пишешь SQL                              │
│  3. supabase db reset (применяешь локально) │
│  4. Тестируешь                              │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│    ПРИМЕНЕНИЕ НА PRODUCTION БД              │
│  Вариант A: Supabase Dashboard → SQL Editor │
│  Вариант B: supabase db push --db-url ...   │
└─────────────────────────────────────────────┘
```

---

## 💡 Советы

1. **Коммить часто** - лучше 10 маленьких коммитов, чем один большой
2. **Тестировать локально** - никогда не пушь код, который не запускал
3. **Писать понятные сообщения** коммитов: "Добавил фильтр дат" вместо "fix"
4. **Делать бэкапы БД** перед большими изменениями
5. **Использовать ветки** для больших фич:
   ```bash
   git checkout -b feature/big-feature
   # работаешь
   git push origin feature/big-feature
   # потом мёрджишь в main через GitHub Pull Request
   ```

---

Теперь ты знаешь всё для работы с проектом на production! 🚀
