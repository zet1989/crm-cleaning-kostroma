# 🧪 Тестирование Webhooks API

## Созданные Endpoints

### 1. `/api/webhooks/site` - Заявки с сайта
**Метод:** `POST`  
**Content-Type:** `application/json`

**Поля:**
- `name` (required) - Имя клиента
- `phone` (required) - Телефон
- `email` (optional) - Email
- `address` (optional) - Адрес
- `message` (optional) - Комментарий
- `cleaning_type` (optional) - Тип уборки
- `scheduled_date` (optional) - Дата (YYYY-MM-DD)
- `scheduled_time` (optional) - Время (HH:MM)

### 2. `/api/webhooks/novofon` - Звонки от Novofon
**Метод:** `POST`  
**Content-Type:** `application/json`

**Поля (от Novofon):**
- `event` - Тип события
- `call_id` - ID звонка
- `direction` - incoming/outgoing
- `from` - Номер звонящего
- `to` - Номер получателя
- `duration` - Длительность (сек)
- `recording_url` - Ссылка на запись
- `answered` - Отвечен ли

### 3. `/api/webhooks/email` - Email заявки
**Метод:** `POST`  
**Content-Type:** `application/json`

**Поля:**
- `name` (optional) - Имя
- `phone` (optional) - Телефон
- `email` (required) - Email
- `subject` (optional) - Тема письма
- `message` (optional) - Текст письма
- `extracted_data` (optional) - AI извлечённые данные

---

## 🚀 Как протестировать

### Способ 1: Тестовая HTML форма (самый простой)

1. Откройте файл `test-form.html` в браузере:
```bash
# Windows
start test-form.html

# Или просто откройте файл в браузере
```

2. Заполните форму и нажмите "Отправить заявку"
3. Проверьте CRM - новая сделка должна появиться в колонке "Новые"

### Способ 2: cURL

**Тест заявки с сайта:**
```bash
curl -X POST http://localhost:3000/api/webhooks/site \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Тестовый",
    "phone": "+7 999 123-45-67",
    "email": "test@example.com",
    "address": "ул. Тестовая 1, кв. 1",
    "message": "Тестовая заявка",
    "cleaning_type": "standard",
    "scheduled_date": "2025-12-15",
    "scheduled_time": "14:00"
  }'
```

**Тест звонка Novofon:**
```bash
curl -X POST http://localhost:3000/api/webhooks/novofon \
  -H "Content-Type: application/json" \
  -d '{
    "event": "call_finished",
    "call_id": "test123",
    "direction": "incoming",
    "from": "+79995554433",
    "to": "+74951234567",
    "duration": 120,
    "answered": true,
    "recording_url": "https://example.com/recording.mp3"
  }'
```

**Тест email заявки:**
```bash
curl -X POST http://localhost:3000/api/webhooks/email \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your_secret_webhook_key_change_me" \
  -d '{
    "name": "Петр Почтовый",
    "phone": "+7 988 777-66-55",
    "email": "petr@mail.ru",
    "subject": "Нужна уборка",
    "message": "Здравствуйте, нужна генеральная уборка квартиры"
  }'
```

### Способ 3: Postman / Insomnia

1. Создайте новый POST запрос
2. URL: `http://localhost:3000/api/webhooks/site`
3. Body → JSON:
```json
{
  "name": "Анна Тестовая",
  "phone": "89991234567",
  "address": "ул. Примерная 10"
}
```
4. Send

---

## ✅ Проверка результата

После отправки запроса:

1. **Откройте CRM** → Канбан → Колонка "Новые"
2. **Найдите новую карточку** с данными из заявки
3. **Проверьте поле `source`**: должно быть `website`, `call` или `email`

**Ожидаемый ответ API (успех):**
```json
{
  "success": true,
  "deal_id": "uuid-сделки",
  "message": "Заявка успешно принята!"
}
```

**Ответ при ошибке:**
```json
{
  "error": "Name and phone are required"
}
```

---

## 🔐 API Key Protection

Если хотите защитить webhooks (рекомендуется для production):

1. В `.env.local` установите:
```env
WEBHOOK_API_KEY=your_strong_secret_key_123
```

2. Добавьте заголовок в запросы:
```
X-API-Key: your_strong_secret_key_123
```

> **Примечание:** Webhook Novofon не требует API ключа, т.к. Novofon использует IP whitelist.

---

## 🐛 Troubleshooting

### Ошибка: "Column 'Новые' not found"
**Решение:** Создайте колонку "Новые" в базе данных:
```sql
INSERT INTO columns (name, position, color) 
VALUES ('Новые', 0, '#3b82f6');
```

### Ошибка: "Unauthorized"
**Решение:** Проверьте API ключ в заголовке или уберите проверку из кода.

### Сделка не появляется в канбане
**Решение:** 
1. Проверьте ответ API - есть ли `deal_id`?
2. Откройте DevTools → Network → проверьте статус ответа
3. Обновите страницу канбана (F5)
4. Проверьте базу данных напрямую

### CORS ошибка
**Решение:** Добавьте заголовки в API routes:
```typescript
export async function POST(request: NextRequest) {
  const response = NextResponse.json({ ... })
  response.headers.set('Access-Control-Allow-Origin', '*')
  return response
}
```

---

## 📝 Следующие шаги

После успешного тестирования webhooks:

1. ✅ **Настроить Real-time обновления** (Supabase Realtime)
   - Автоматическое появление новых карточек без перезагрузки
   - Toast уведомления
   - Звуковой сигнал

2. ✅ **Интегрировать форму на реальный сайт**
   - Скопировать код из `test-form.html`
   - Заменить URL на production

3. ✅ **Настроить Novofon**
   - Получить API ключи Novofon
   - Добавить webhook URL в личном кабинете
   - Протестировать реальный звонок

4. ✅ **Настроить n8n для Email** (опционально)
   - Установить n8n
   - Создать workflow для обработки писем
   - Подключить AI для извлечения данных

---

## 📊 Мониторинг

Логи webhook запросов смотрите в терминале Next.js:
```
[WEBHOOK:SITE] New deal created: abc-123 - Иван Тестовый (+79991234567)
[WEBHOOK:NOVOFON] Event: call_finished, Call ID: xyz-789
[WEBHOOK:EMAIL] New deal created: def-456 - Петр (petr@mail.ru)
```

Для production рекомендуется использовать полноценное логирование (Winston, Pino).
